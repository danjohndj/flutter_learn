<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DashBoaard.Views.Communication">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Text="Phone Number"/>
            <Entry Grid.Row="1" Grid.Column="0" x:Name="txtPhone" />
        </Grid>

    </ContentPage.Content>

    <ContentPage.ToolbarItems>

        <ToolbarItem Text="Call" IconImageSource="outline_call_black_24.png" />
        <ToolbarItem Text="SMS" IconImageSource="outline_textsms_black_24" />
        <ToolbarItem Text="Save" IconImageSource="outline_save_black_24" />

    </ContentPage.ToolbarItems>

</ContentPage>